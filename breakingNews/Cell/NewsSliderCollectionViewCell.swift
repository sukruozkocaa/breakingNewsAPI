//
//  NewsSliderCollectionViewCell.swift
//  breakingNews
//
//  Created by Şükrü Özkoca on 31.08.2022.
//

import UIKit

class NewsSliderCollectionViewCell: UICollectionViewCell {
    @IBOutlet var pictureView: UIImageView!
    
    /*
    func colleciton(item : Article) {
        pictureView.sd_setImage(with: URL(string: item.urlToImage ?? ""), placeholderImage: UIImage(named: "dummy"))
        pictureView.layer.cornerRadius = 5
        pictureView.layer.shadowColor = UIColor.systemPink.cgColor
        pictureView.layer.shadowOpacity = 1
        pictureView.layer.shadowOffset = .zero
        pictureView.layer.shadowRadius = 10
    }
     */
}
